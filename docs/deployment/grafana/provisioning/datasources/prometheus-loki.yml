apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    org_id: 1
    url: http://prometheus:9090
    is_default: true
    editable: true

  - name: Loki
    type: loki
    access: proxy
    org_id: 1
    url: http://loki:3100
    editable: true
