# Telegraf Installation

The `docs/scripts/install-telegraf.sh` script automates the installation and configuration of Telegraf on fleet nodes.

## Usage
```bash
./docs/scripts/install-telegraf.sh <INFLUX_TOKEN>
```
