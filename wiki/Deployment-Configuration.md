# Deployment Configuration

Contains the `docker-compose.yml` and all necessary configuration files (Prometheus, Grafana, Loki, etc.) for the central monitoring stack on `docker2`.

## Deployment
1. Navigate to `docs/deployment/`.
2. Run `docker compose up -d`.
