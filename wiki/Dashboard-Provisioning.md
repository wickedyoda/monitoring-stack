# Dashboard Provisioning

The `docs/scripts/provision-dashboard.sh` script automates the deployment of Grafana dashboards via the Grafana API.

## Usage
```bash
./docs/scripts/provision-dashboard.sh <GRAFANA_URL> <API_TOKEN> <DASHBOARD_JSON_FILE>
```
